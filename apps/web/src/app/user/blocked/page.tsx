import BlockedUser from "@/components/user/auth/BlockedUser";
import Wrapper from "@/components/wrapper";

export default function BlockedPage() {
    return (
        <Wrapper>
            <BlockedUser />
        </Wrapper>
    );
}